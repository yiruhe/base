package extend;

public class Extend {

	//访问权限： private<default<protected<public
	
	/*
	 * public 哪里都可以访问
	 * protected 只要有血缘关系就可以访问
	 * default 只要在同一个包下
	 * private 只能内部调用
	 * 
	 * 
	 *类的访问权限 方法 加了public 到处就可以访问
	 * 没加  只能  本包访问
	 * */
	
}
